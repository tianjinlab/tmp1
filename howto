步骤如下：
1./git/git config --global user.email artin.ren@outlook.com
2./git/git config --global user.name "tianjinlab"
3./git/git init
4.git add XXX
5.git commit -m "first commit"
6.git remote add origin  https://github.com/tianjinlab/tmp1.git
7.git push -u origin master

第一次常用:
1. git pull https://github.com/tianjinlab/tmp1.git master
2. git push https://github.com/tianjinlab/tmp1.git master

每当修改完一个项目时，git add是把这个项目加到本地仓库，这时git push是不会成功的。需要再git commit打上一个标签，才能说有改动。然后在git push。
